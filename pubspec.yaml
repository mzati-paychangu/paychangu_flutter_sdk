name: paychangu_flutter
description: "A Flutter SDK for integrating PayChangu payment gateway. Supports hosted checkout, direct mobile money, bank transfer, cards, payouts, bills, Connect, and US virtual accounts."
version: 1.0.0
homepage: https://github.com/paychangu/paychangu-flutter
repository: https://github.com/paychangu/paychangu-flutter
documentation: https://developer.paychangu.com

environment:
  sdk: ^3.5.0
  flutter: ">=3.24.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.2
  webview_flutter: ^4.10.0
  crypto: ^3.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:

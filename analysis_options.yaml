include: package:flutter_lints/flutter.yaml

analyzer:
  exclude:
    - build/**
    - example/build/**

linter:
  rules:
    # Currency ISO codes (MWK, USD) are intentional public API names.
    constant_identifier_names: false
